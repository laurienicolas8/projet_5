{% extends 'template_frontend.twig' %}

{% block head %}
<title>Quiz</title>
{% endblock %}

{% block content %}


{% for quiz in oneQuiz %}
<a href="javascript:history.back()" class="back_arrow_start"><i class="fa fa-arrow-left fa-2x" aria-hidden="true"></i></a>
<h1 class="title_start_quiz">{{ quiz.nameQuiz }}</h1>

<div class="container col-lg-11">
    <div class="row">
        {% for countQuestions in oneCount %}
        <div class="col-lg-12 p_start_quiz">{{ countQuestions }} questions</div>
        {% endfor %}
        <div class="col-lg-12 start_quiz_container">
            <img class="rounded" src="public/images/img_quiz/{{ quiz.imageQuiz }}" style="max-width: 35%;" alt="Image représentant le quiz">
            <a href="/projet_5/quiz-questions-{{ quiz.idQuiz }}" class="btn-success btn_start_quiz">Commencer le quiz<i class="fa fa-chevron-right fa-3x" aria-hidden="true"></i></a>
        </div>
    </div>
</div>
{% endfor %}
    
<!-- END ROW QUIZ -->
</div>

{% endblock %}